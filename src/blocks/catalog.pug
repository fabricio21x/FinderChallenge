
ul.book-catalog
  li.catalog-item.pure-u-1-3(dir-paginate="book in filteredObj =  (data | filter:filterBooks)| itemsPerPage: 6")
    img(ng-src="{{book.image}}" alt="Cover")
    .item-content
      .item-title {{book.title}}
      .item-teaser {{book.teaser}}
